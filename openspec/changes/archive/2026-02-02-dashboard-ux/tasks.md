# Tasks: Dashboard UX Polish

## 1. Structure & Layout
- [ ] 1.1 Update HTML structure in `main.ts`/`index.html` to introduce Sidebar + Main Content layout.
- [ ] 1.2 Update `style.css` to support Grid Layout (Sidebar fixed, fluid content).

## 2. Components
- [ ] 2.1 Implement `showToast(msg, type)` function and CSS.
- [ ] 2.2 Implement KPI Cards HTML generation logic in `main.ts` (calculate from `appointments`).
- [ ] 2.3 Refine "Nova Consulta" Modal to be more elegant (smooth fade-in).

## 3. Interaction
- [ ] 3.1 Update FullCalendar `eventClick` to open an "Event Details" modal (Read-only for now).
- [ ] 3.2 Verify responsiveness (Sidebar collapses on mobile?).
