# Tasks: CRUD Implementation

## Backend
- [ ] 1.1 Implement `DELETE /api/appointments/{id}` in `src/main.py`.
- [ ] 1.2 Implement `PUT /api/appointments/{id}` and `AppointmentUpdate` model in `src/main.py`.

## Frontend
- [ ] 2.1 Add `deleteAppointment` and `updateAppointment` to `api.ts`.
- [ ] 2.2 Add "Excluir" button logic to Details Modal in `main.ts`.
- [ ] 2.3 Add "Editar" button logic (populates form) in `main.ts`.
